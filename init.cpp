#include "custom.h"
#include <vector>

void initialize(sf::Sprite &player, sf::View camera, std::vector<block> &sprites)
{
    // player init
    player.setPosition(0, -100);

        // STUB UNTIL MAIN MENU IS IMPLEMENTED
        /*
        block newBlock;
        newBlock.setOrigin(40, 40);
        newBlock.startPoint = true;
        newBlock.setPosition(0, 0);

        //newBlock.setTexture(whiteBox);
        sprites.push_back(newBlock);
        */

}
